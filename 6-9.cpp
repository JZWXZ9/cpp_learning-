// 使用三元关系 ? 
#include <iostream>
using namespace std ;
int main() {
    int a , b ; 
    cin >> a >> b ;
    cin.get() ;
    cout << "The larger one is : " << (a > b ? a : b) ;

    cin.get() ;
    return 0 ;
}